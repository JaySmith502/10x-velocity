---
phase: 05-content-structure-images
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/VisualBreadcrumb.tsx
  - src/pages/Services.tsx
  - src/pages/services/PhoneVoiceAgents.tsx
autonomous: true

must_haves:
  truths:
    - "Services page displays a visible FAQ section with 4 Q&A pairs in expandable accordions"
    - "Phone Voice Agents page displays a visible FAQ section with 4 Q&A pairs in expandable accordions"
    - "A reusable VisualBreadcrumb component exists that renders linked breadcrumb trails from a BreadcrumbItem[] array"
    - "Services page shows breadcrumb: Home > Services"
    - "Phone Voice Agents page shows breadcrumb: Home > Services > Phone Voice Agents"
  artifacts:
    - path: "src/components/VisualBreadcrumb.tsx"
      provides: "Reusable visual breadcrumb navigation component"
      exports: ["VisualBreadcrumb"]
    - path: "src/pages/Services.tsx"
      provides: "FAQ section with Accordion + visual breadcrumb"
      contains: "Frequently Asked Questions"
    - path: "src/pages/services/PhoneVoiceAgents.tsx"
      provides: "FAQ section with Accordion + visual breadcrumb"
      contains: "Frequently Asked Questions"
  key_links:
    - from: "src/components/VisualBreadcrumb.tsx"
      to: "src/components/ui/breadcrumb.tsx"
      via: "imports shadcn Breadcrumb primitives"
      pattern: "from.*@/components/ui/breadcrumb"
    - from: "src/components/VisualBreadcrumb.tsx"
      to: "react-router-dom"
      via: "Link component with asChild for SPA navigation"
      pattern: "asChild"
    - from: "src/pages/Services.tsx"
      to: "FAQPage JSON-LD schema"
      via: "shared faqs array used by both visible Accordion and schema"
      pattern: "faqs\\.map"
---

<objective>
Create the reusable VisualBreadcrumb component and add visible FAQ sections to Services and PhoneVoiceAgents pages.

Purpose: The breadcrumb component is the foundation for CONT-01 (used by all 27+ interior pages in Plan 02). The FAQ sections fulfill CONT-02 by making existing schema-only FAQ content visible to users.

Output: VisualBreadcrumb.tsx component, FAQ Accordion sections on 2 pages, breadcrumb examples on those same 2 pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-content-structure-images/05-RESEARCH.md
@src/schemas/breadcrumbs.ts
@src/components/ui/breadcrumb.tsx
@src/components/ui/accordion.tsx
@src/pages/services/AIWorkshops.tsx
@src/pages/Services.tsx
@src/pages/services/PhoneVoiceAgents.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VisualBreadcrumb component</name>
  <files>src/components/VisualBreadcrumb.tsx</files>
  <action>
Create `src/components/VisualBreadcrumb.tsx` -- a reusable component that renders a visual breadcrumb trail.

Interface: Accept `items: BreadcrumbItem[]` where `BreadcrumbItem` is `{ name: string; path: string }` -- the SAME shape used by `breadcrumbJsonLd()` in `src/schemas/breadcrumbs.ts`. Import that interface or redefine it identically.

Implementation:
- Import `Breadcrumb`, `BreadcrumbList`, `BreadcrumbItem`, `BreadcrumbLink`, `BreadcrumbPage`, `BreadcrumbSeparator` from `@/components/ui/breadcrumb`
- Import `Link` from `react-router-dom`
- Import `Fragment` from `react`
- Map over items array:
  - For each item except the last: render `BreadcrumbLink` with `asChild` prop wrapping a React Router `<Link to={item.path}>` (this ensures SPA navigation, not full page reloads)
  - For the last item: render `BreadcrumbPage` (renders as span with aria-current="page")
  - Between items: render `BreadcrumbSeparator` (renders ChevronRight icon by default)
- Wrap the entire component in a `<div className="container mx-auto px-4 pt-6">` so it sits below the header with consistent page padding
- Export as named export: `export function VisualBreadcrumb`

Styling: The shadcn breadcrumb primitives already provide appropriate text-sm text-muted-foreground styling. Do NOT override with custom colors. The existing styles work well on the dark theme.

Do NOT import from `@/schemas/breadcrumbs.ts` -- just define the same `BreadcrumbItem` interface locally to avoid coupling the visual component to the schema helper.
  </action>
  <verify>
Run `npm run build` -- no TypeScript errors. Visually inspect the component file to confirm: uses asChild with Link, handles last-item as BreadcrumbPage, exports VisualBreadcrumb.
  </verify>
  <done>VisualBreadcrumb.tsx exists, exports a working component that accepts BreadcrumbItem[] and renders linked breadcrumb trail using shadcn primitives with React Router Links.</done>
</task>

<task type="auto">
  <name>Task 2: Add FAQ sections and breadcrumbs to Services and PhoneVoiceAgents</name>
  <files>src/pages/Services.tsx, src/pages/services/PhoneVoiceAgents.tsx</files>
  <action>
**Services.tsx changes:**

1. Extract FAQ content from the inline FAQPage schema (lines ~27-60) into a `const faqs` array at the bottom of the file (alongside the existing `services` array). Each item: `{ question: string; answer: string }`. There are 4 Q&A pairs:
   - "What AI and automation services does 10x Velocity offer?"
   - "How long does it take to see results from AI automation?"
   - "Do I need technical expertise to work with 10x Velocity?"
   - "What industries does 10x Velocity serve?"

2. Replace the inline FAQ objects in the FAQPage schema with a reference to the `faqs` array:
   ```
   mainEntity: faqs.map(faq => ({
     "@type": "Question",
     name: faq.question,
     acceptedAnswer: { "@type": "Answer", text: faq.answer },
   })),
   ```

3. Add a visible FAQ section before the CTA section (before "Ready to Transform Your Operations?"). Replicate the exact AIWorkshops pattern:
   ```tsx
   <section className="container mx-auto px-4 py-16">
     <div className="max-w-4xl mx-auto">
       <h3 className="text-3xl font-bold mb-8 text-center">Frequently Asked Questions</h3>
       <div className="glass-card p-8">
         <Accordion type="single" collapsible className="w-full">
           {faqs.map((faq, index) => (
             <AccordionItem key={index} value={`item-${index}`}>
               <AccordionTrigger className="text-left">{faq.question}</AccordionTrigger>
               <AccordionContent>{faq.answer}</AccordionContent>
             </AccordionItem>
           ))}
         </Accordion>
       </div>
     </div>
   </section>
   ```

4. Import Accordion components: `import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion";`

5. Import VisualBreadcrumb: `import { VisualBreadcrumb } from "@/components/VisualBreadcrumb";`

6. Add `<VisualBreadcrumb items={[{ name: "Home", path: "/" }, { name: "Services", path: "/services" }]} />` as the first child inside the root `<div className="min-h-screen flex flex-col">`, before the Hero section.

**PhoneVoiceAgents.tsx changes:**

1. Extract FAQ content from the inline FAQPage schema (lines ~25-58) into a `const faqs` array at the bottom of the file (alongside existing `benefits` and `features` arrays). 4 Q&A pairs:
   - "How do AI voice agents handle customer calls?"
   - "Can AI voice agents work with my existing phone system?"
   - "What languages do the AI voice agents support?"
   - "How much can AI voice agents reduce my call center costs?"

2. Replace the inline FAQ objects in the FAQPage schema with a reference to the `faqs` array (same pattern as Services).

3. Add a visible FAQ section before the CTA section (before "Ready to Transform Your Customer Communication?"). Use the exact same Accordion pattern as above.

4. Import Accordion components: `import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion";`

5. Import VisualBreadcrumb: `import { VisualBreadcrumb } from "@/components/VisualBreadcrumb";`

6. Add `<VisualBreadcrumb items={[{ name: "Home", path: "/" }, { name: "Services", path: "/services" }, { name: "Phone Voice Agents", path: "/services/phone-voice-agents" }]} />` as the first child inside `<main className="flex-1">`, before the Hero section.

CRITICAL: The `faqs` arrays must be the single source of truth -- referenced by both the visible Accordion AND the FAQPage JSON-LD schema. Do NOT duplicate the content.
  </action>
  <verify>
Run `npm run build` -- no TypeScript errors. Run `npm run dev` and visit:
- `/services` -- confirm FAQ section visible with 4 expandable Q&A pairs, breadcrumb shows "Home > Services"
- `/services/phone-voice-agents` -- confirm FAQ section visible with 4 expandable Q&A pairs, breadcrumb shows "Home > Services > Phone Voice Agents"
  </verify>
  <done>Both pages show visible FAQ sections with expandable accordions containing the same content as their FAQPage JSON-LD schemas. Both pages show visual breadcrumb trails. FAQ data is single-sourced (one array serves both visible UI and schema).</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `/services` page shows breadcrumb trail and FAQ accordion with 4 Q&A pairs
3. `/services/phone-voice-agents` page shows breadcrumb trail and FAQ accordion with 4 Q&A pairs
4. View page source on both pages to confirm FAQPage JSON-LD still renders correctly (schema not broken by refactor)
5. Breadcrumb links navigate via SPA (no full page reload) when clicking "Home" or "Services"
</verification>

<success_criteria>
- VisualBreadcrumb component created and exported
- Services page has visible FAQ section with 4 Q&A pairs in Accordion
- PhoneVoiceAgents page has visible FAQ section with 4 Q&A pairs in Accordion
- FAQ data is single-sourced (one `faqs` array per page, used by both Accordion and schema)
- Breadcrumbs render correctly on both pages with working SPA links
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-structure-images/05-01-SUMMARY.md`
</output>
