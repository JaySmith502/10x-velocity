---
phase: 03-social-open-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/pages/Index.tsx
  - src/pages/About.tsx
  - src/pages/Services.tsx
  - src/pages/CaseStudies.tsx
  - src/pages/Blog.tsx
  - src/pages/Contact.tsx
  - src/pages/SavingsCalculator.tsx
  - src/pages/IndustryTools.tsx
  - src/pages/Prototypes.tsx
  - src/pages/PowerAutomate.tsx
  - src/pages/LexiFile.tsx
  - src/pages/LunchAndLearn.tsx
  - src/pages/OnboardingForm.tsx
  - src/pages/AIGuideCertification.tsx
  - src/pages/services/PhoneVoiceAgents.tsx
  - src/pages/services/DataCleaning.tsx
  - src/pages/services/AIWorkshops.tsx
  - src/pages/SmartBots.tsx
  - src/pages/PrivacyPolicy.tsx
  - src/pages/TermsOfService.tsx
  - src/pages/case-studies/InnesYoung.tsx
  - src/pages/case-studies/ECatalyst.tsx
  - src/pages/case-studies/HillcrestPartners.tsx
  - src/pages/case-studies/CatalystGroup.tsx
  - src/pages/case-studies/DirectorOfMarketing.tsx
  - src/pages/case-studies/BirchwoodRealEstate.tsx
  - src/pages/case-studies/GovBrokers.tsx
  - src/pages/case-studies/Inspyrd.tsx
  - src/pages/case-studies/TransportationDirector.tsx
  - src/components/blog/BlogPost.tsx
autonomous: true

must_haves:
  truths:
    - "Every page's HTML head contains og:title, og:description, og:url, og:type, og:image, and twitter:card meta tags"
    - "OG tag values match the existing title, description, and canonical URL on each page"
    - "og:image uses absolute URL https://10xvelocity.ai/og-image.png on all static pages"
    - "Blog posts use og:type article and their Unsplash post.image as og:image"
    - "index.html static fallback og:image uses absolute URL"
    - "twitter:card is set to summary_large_image on every page"
  artifacts:
    - path: "index.html"
      provides: "Static fallback OG tags for social crawlers that do not execute JS"
      contains: "https://10xvelocity.ai/og-image.png"
    - path: "src/pages/Index.tsx"
      provides: "Homepage OG and Twitter Card meta tags"
      contains: "og:title"
    - path: "src/components/blog/BlogPost.tsx"
      provides: "Dynamic blog OG tags with article type and post-specific image"
      contains: "og:type"
  key_links:
    - from: "each page Helmet block"
      to: "existing title/description/canonical values"
      via: "OG tags reuse same string values"
      pattern: "og:title.*content=.*10x Velocity"
    - from: "index.html"
      to: "public/og-image.png"
      via: "absolute URL in static meta tag"
      pattern: "https://10xvelocity\\.ai/og-image\\.png"
    - from: "BlogPost.tsx"
      to: "post.image"
      via: "dynamic og:image using Unsplash URL"
      pattern: "og:image.*content=.post\\.image"
---

<objective>
Add Open Graph and Twitter Card meta tags to all 30 pages, and fix the index.html static OG image to use an absolute URL.

Purpose: Enable rich social media previews when any page is shared on Facebook, LinkedIn, or Twitter/X. The static fallback tags in index.html ensure crawlers that do not execute JavaScript (all social crawlers) still get baseline metadata.

Output: Every page has 6 new meta tags in its Helmet block (og:title, og:description, og:url, og:type, og:image, twitter:card). index.html has updated absolute-URL fallback tags.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-social-open-graph/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OG and Twitter Card meta tags to index.html fallback and all 20 static pages</name>
  <files>
    index.html
    src/pages/Index.tsx
    src/pages/About.tsx
    src/pages/Services.tsx
    src/pages/CaseStudies.tsx
    src/pages/Blog.tsx
    src/pages/Contact.tsx
    src/pages/SavingsCalculator.tsx
    src/pages/IndustryTools.tsx
    src/pages/Prototypes.tsx
    src/pages/PowerAutomate.tsx
    src/pages/LexiFile.tsx
    src/pages/LunchAndLearn.tsx
    src/pages/OnboardingForm.tsx
    src/pages/AIGuideCertification.tsx
    src/pages/services/PhoneVoiceAgents.tsx
    src/pages/services/DataCleaning.tsx
    src/pages/services/AIWorkshops.tsx
    src/pages/SmartBots.tsx
    src/pages/PrivacyPolicy.tsx
    src/pages/TermsOfService.tsx
  </files>
  <action>
    **index.html changes:**
    1. Change the existing `<meta property="og:image" content="/og-image.png" />` to `<meta property="og:image" content="https://10xvelocity.ai/og-image.png" />` (absolute URL, fixes IMG-04).
    2. Add these additional static fallback tags right after the og:image tag:
       ```html
       <meta property="og:title" content="10x Velocity | AI & Automation Consulting" />
       <meta property="og:description" content="10x Velocity helps businesses automate workflows and leverage AI to save time and cut costs." />
       <meta property="og:url" content="https://10xvelocity.ai/" />
       <meta property="og:type" content="website" />
       <meta name="twitter:card" content="summary_large_image" />
       ```
       These serve as baseline fallbacks for social crawlers that do not execute JavaScript. React Helmet will override them at runtime for JS-enabled clients.

    **Per-page Helmet changes (20 static pages):**
    For each page file, add 6 meta tags inside the existing `<Helmet>` block, right after the existing `<link rel="canonical" .../>` line. The values are derived from the EXISTING title, description, and canonical values already in each Helmet block -- do NOT change those existing values, only add the new OG/Twitter tags below them.

    Pattern for every static page:
    ```tsx
    <meta property="og:title" content="[SAME AS THE TITLE TAG VALUE]" />
    <meta property="og:description" content="[SAME AS THE META DESCRIPTION VALUE]" />
    <meta property="og:url" content="[SAME AS THE CANONICAL HREF VALUE]" />
    <meta property="og:type" content="website" />
    <meta property="og:image" content="https://10xvelocity.ai/og-image.png" />
    <meta name="twitter:card" content="summary_large_image" />
    ```

    CRITICAL RULES:
    - og:title must exactly match the existing `<title>` value (including "| 10x Velocity" suffix)
    - og:description must exactly match the existing `<meta name="description" content="...">` value
    - og:url must exactly match the existing `<link rel="canonical" href="...">` value (homepage uses trailing slash, all others do not)
    - og:type is "website" for ALL 20 static pages
    - og:image is always `https://10xvelocity.ai/og-image.png` (absolute URL)
    - twitter:card is always `summary_large_image`
    - Do NOT add twitter:title, twitter:description, or twitter:image (Twitter falls back to OG tags)
    - Do NOT add twitter:site (company has no Twitter handle)
    - Do NOT modify any existing tags -- only ADD the 6 new ones
    - The SavingsCalculator.tsx Helmet is inside a TooltipProvider -- add the tags inside that existing Helmet block as usual

    Work through the 20 files systematically. The complete list of files and their existing Helmet values is documented in the research file at `.planning/phases/03-social-open-graph/03-RESEARCH.md` under "All 30 Pages with Helmet."
  </action>
  <verify>
    Run `npm run build` to confirm no TypeScript or JSX errors.
    Then grep to confirm all 20 static page files + index.html contain og:title:
    - `grep -rl "og:title" src/pages/ index.html` should return 21 files (20 pages + index.html)
    - `grep -rl "og:image.*https://10xvelocity.ai/og-image.png" index.html` confirms absolute URL in fallback
    - `grep -c "twitter:card" src/pages/Index.tsx` should return 1
  </verify>
  <done>
    All 20 static page Helmet blocks contain og:title, og:description, og:url, og:type, og:image, and twitter:card meta tags with correct values. index.html og:image uses absolute URL https://10xvelocity.ai/og-image.png and contains static fallback OG tags. Build succeeds with no errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add OG and Twitter Card meta tags to 9 case study pages and BlogPost component</name>
  <files>
    src/pages/case-studies/InnesYoung.tsx
    src/pages/case-studies/ECatalyst.tsx
    src/pages/case-studies/HillcrestPartners.tsx
    src/pages/case-studies/CatalystGroup.tsx
    src/pages/case-studies/DirectorOfMarketing.tsx
    src/pages/case-studies/BirchwoodRealEstate.tsx
    src/pages/case-studies/GovBrokers.tsx
    src/pages/case-studies/Inspyrd.tsx
    src/pages/case-studies/TransportationDirector.tsx
    src/components/blog/BlogPost.tsx
  </files>
  <action>
    **Case study pages (9 files):**
    Apply the same pattern as Task 1 to each case study page's existing Helmet block. Add the 6 meta tags after the canonical link:
    ```tsx
    <meta property="og:title" content="[SAME AS TITLE TAG]" />
    <meta property="og:description" content="[SAME AS META DESCRIPTION]" />
    <meta property="og:url" content="[SAME AS CANONICAL HREF]" />
    <meta property="og:type" content="website" />
    <meta property="og:image" content="https://10xvelocity.ai/og-image.png" />
    <meta name="twitter:card" content="summary_large_image" />
    ```
    All 9 case study pages use og:type "website" and the site-wide og-image.png.

    **BlogPost.tsx (1 file -- dynamic, different pattern):**
    This component uses dynamic values from the `post` object. Add the OG/Twitter tags inside the existing `<Helmet>` block, after the canonical link. Use the existing dynamic variables:
    ```tsx
    <meta property="og:title" content={`${blogTitle} | 10x Velocity`} />
    <meta property="og:description" content={post.excerpt} />
    <meta property="og:url" content={`https://10xvelocity.ai/blog/${post.id}`} />
    <meta property="og:type" content="article" />
    <meta property="og:image" content={post.image} />
    <meta name="twitter:card" content="summary_large_image" />
    ```

    KEY DIFFERENCES for BlogPost vs static pages:
    - og:type is "article" (not "website") because blog posts are articles
    - og:image uses `post.image` (Unsplash URL, already absolute) instead of the site-wide og-image.png
    - All values use JSX expressions matching the existing dynamic title/description/canonical pattern
    - `blogTitle` is the existing truncated title variable (already defined in the component)
  </action>
  <verify>
    Run `npm run build` to confirm no TypeScript or JSX errors.
    Grep to confirm:
    - `grep -rl "og:title" src/pages/case-studies/` should return 9 files
    - `grep -c "og:type.*article" src/components/blog/BlogPost.tsx` should return 1
    - `grep -c "og:image.*post.image" src/components/blog/BlogPost.tsx` should return 1
    - Total OG-tagged files: `grep -rl "og:title" src/` should return 30 files (20 static + 9 case studies + 1 BlogPost)
  </verify>
  <done>
    All 9 case study pages have OG and Twitter Card meta tags with correct static values. BlogPost.tsx has dynamic OG tags with og:type "article" and post-specific og:image. Build succeeds. All 30 page components now have complete OG and Twitter Card meta tags.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors.
2. `grep -rl "og:title" src/ index.html | wc -l` returns 31 (30 page components + index.html).
3. `grep -rl "twitter:card" src/ index.html | wc -l` returns 31.
4. `grep "og:image.*https://10xvelocity.ai/og-image.png" index.html` confirms absolute URL fallback.
5. Start dev server (`npm run dev`), visit http://localhost:8080, inspect the `<head>` in browser DevTools -- confirm og:title, og:description, og:url, og:type, og:image, and twitter:card are present.
6. Visit http://localhost:8080/blog/1 -- confirm og:type is "article" and og:image uses the Unsplash URL (not the site-wide og-image.png).

Note: Social crawlers (Facebook, LinkedIn, Twitter) do NOT execute JavaScript and will only see the static fallback tags from index.html until Netlify Prerendering is enabled. This is a known limitation documented in the research. The Helmet-injected per-page OG tags are correct and will work once prerendering is active.
</verification>

<success_criteria>
- SOCL-01 satisfied: Every page has og:title, og:description, og:url, og:type, og:image with absolute URLs
- SOCL-02 satisfied: Every page has twitter:card set to summary_large_image (Twitter falls back to OG for title/description/image)
- IMG-04 satisfied: index.html og:image uses https://10xvelocity.ai/og-image.png (absolute URL)
- Build passes with zero errors
- All 30 page components + index.html contain the required meta tags
</success_criteria>

<output>
After completion, create `.planning/phases/03-social-open-graph/03-01-SUMMARY.md`
</output>
