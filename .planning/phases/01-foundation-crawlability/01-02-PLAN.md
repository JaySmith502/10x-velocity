---
phase: 01-foundation-crawlability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Contact.tsx
  - src/pages/Blog.tsx
  - src/pages/PrivacyPolicy.tsx
  - src/pages/TermsOfService.tsx
  - src/components/landing/Footer.tsx
autonomous: true

must_haves:
  truths:
    - "All email addresses on the site read info@10xvelocity.ai (not .com)"
    - "Footer copyright year updates automatically (shows current year, not hardcoded 2024)"
    - "All external links in the footer open in new tabs with noopener noreferrer attributes"
    - "FIX-02 (LeadConnector form privacy/terms links) is documented as requiring manual GoHighLevel config"
  artifacts:
    - path: "src/pages/Contact.tsx"
      provides: "Corrected email address"
      contains: "info@10xvelocity.ai"
    - path: "src/pages/Blog.tsx"
      provides: "Corrected email address"
      contains: "info@10xvelocity.ai"
    - path: "src/pages/PrivacyPolicy.tsx"
      provides: "Corrected email address"
      contains: "info@10xvelocity.ai"
    - path: "src/pages/TermsOfService.tsx"
      provides: "Corrected email address"
      contains: "info@10xvelocity.ai"
    - path: "src/components/landing/Footer.tsx"
      provides: "Dynamic copyright year and proper external link tags"
      contains: "new Date().getFullYear()"
  key_links:
    - from: "src/components/landing/Footer.tsx"
      to: "https://resources.10xvelocity.ai/"
      via: "External <a> tags with target=_blank and rel=noopener noreferrer"
      pattern: "<a.*href=\"https://resources\\.10xvelocity\\.ai"
---

<objective>
Fix all content issues across the site: standardize email addresses to info@10xvelocity.ai, make the copyright year dynamic, and convert the footer's external links from React Router `<Link>` to proper `<a>` tags with security attributes.

Purpose: These are quick-win SEO and correctness fixes. Wrong email addresses lose leads. Hardcoded copyright years signal an unmaintained site. React Router `<Link>` on external URLs breaks navigation and lacks noopener/noreferrer security attributes.

Output: Five modified source files with all content issues resolved.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-crawlability/01-RESEARCH.md

Key source files to modify:
@src/pages/Contact.tsx
@src/pages/Blog.tsx
@src/pages/PrivacyPolicy.tsx
@src/pages/TermsOfService.tsx
@src/components/landing/Footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix email addresses across four pages</name>
  <files>
    src/pages/Contact.tsx
    src/pages/Blog.tsx
    src/pages/PrivacyPolicy.tsx
    src/pages/TermsOfService.tsx
  </files>
  <action>
In each of the four files below, replace ALL occurrences of `info@10xvelocity.com` with `info@10xvelocity.ai`. This includes both `href="mailto:..."` attribute values AND visible display text.

**Contact.tsx** (around line 20):
- Change `mailto:info@10xvelocity.com` to `mailto:info@10xvelocity.ai`
- Change the display text `info@10xvelocity.com` to `info@10xvelocity.ai`

**Blog.tsx** (around line 82):
- Change `mailto:info@10xvelocity.com` to `mailto:info@10xvelocity.ai`
- Change the display text `info@10xvelocity.com` to `info@10xvelocity.ai`

**PrivacyPolicy.tsx** (around line 153):
- Change the display text `info@10xvelocity.com` to `info@10xvelocity.ai`
- Also check for any `mailto:` href on the same element and fix it too

**TermsOfService.tsx** (around line 109):
- Change the display text `info@10xvelocity.com` to `info@10xvelocity.ai`
- Also check for any `mailto:` href on the same element and fix it too

After making changes, search each file for any remaining `.com` references to make sure nothing was missed. Do NOT change any other content in these files.
  </action>
  <verify>
    - Search all four modified files for "10xvelocity.com" -- should return zero matches
    - Search all four modified files for "10xvelocity.ai" -- should find the corrected email in each
    - Run `npm run build` to confirm no build errors
  </verify>
  <done>
    - All four files contain `info@10xvelocity.ai` where they previously had `.com`
    - Zero occurrences of `info@10xvelocity.com` remain anywhere in the `src/` directory
    - The project builds successfully
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Footer -- dynamic copyright year and external link tags</name>
  <files>
    src/components/landing/Footer.tsx
  </files>
  <action>
Make three targeted changes to `src/components/landing/Footer.tsx`:

**Change 1: Dynamic copyright year (line 75)**
Replace the hardcoded copyright text:
```tsx
// FROM:
© 2024 10x Velocity. All rights reserved.
// TO:
© {new Date().getFullYear()} 10x Velocity. All rights reserved.
```
Note: The `©` is an HTML entity and should remain as-is (or as the `&copy;` equivalent). Only the year `2024` changes to `{new Date().getFullYear()}`.

**Change 2: Convert external links from `<Link>` to `<a>` (lines 59-61)**
Replace the three `<Link to="https://...">` elements in the Resources section with proper `<a>` tags:

```tsx
// FROM (line 59):
<li><Link to="https://resources.10xvelocity.ai/" className="text-velocity-muted hover:text-velocity-accent transition-colors">Documentation</Link></li>

// TO:
<li><a href="https://resources.10xvelocity.ai/" target="_blank" rel="noopener noreferrer" className="text-velocity-muted hover:text-velocity-accent transition-colors">Documentation</a></li>
```

Apply the same pattern to line 60 (Playbooks) and line 61 (Blog -- href is `https://resources.10xvelocity.ai/blog`).

Each converted tag must have:
- `href` instead of `to`
- `target="_blank"`
- `rel="noopener noreferrer"`
- Preserved className and display text

**Change 3: Clean up unused Link import (if applicable)**
After converting the three external links, check if `Link` from `react-router-dom` is still used elsewhere in the file (lines 16, 31, 44-51, 57-58, 67-69). It IS still used for internal links like `/about`, `/services`, `/case-studies`, etc. So do NOT remove the `import { Link } from "react-router-dom"` statement. Only remove it if no other `<Link>` elements remain in the file (which is NOT the case here).

**FIX-02 note (LeadConnector form links):**
FIX-02 requires changing privacy/terms links inside LeadConnector embedded forms. This CANNOT be done in code -- the forms are loaded via iframe from `api.leadconnectorhq.com`. The fix requires logging into the GoHighLevel admin dashboard and updating form settings for:
- Form ID `mYtM8nnkSBtAzcDroeEO` (popup contact form)
- Form ID `oF3UAG9Kp3vwDm8QCJ1i` (contact page form)

Document this in the SUMMARY as a manual step the user must complete.
  </action>
  <verify>
    - Search Footer.tsx for `<Link to="https://` -- should return zero matches (all external links now use `<a>`)
    - Search Footer.tsx for `2024` -- should return zero matches (year is now dynamic)
    - Search Footer.tsx for `noopener noreferrer` -- should find 4 matches (3 new resource links + 1 existing Canopy link)
    - Run `npm run build` to confirm no build errors
    - Run `npm run dev` and visually confirm the footer shows the current year (2026) in the copyright line
  </verify>
  <done>
    - Footer copyright dynamically shows current year via `new Date().getFullYear()`
    - All three external resource links use `<a>` tags with `target="_blank"` and `rel="noopener noreferrer"`
    - No `<Link>` components are used for external URLs anywhere in the footer
    - The `Link` import is preserved because internal `<Link>` elements still exist
    - FIX-02 (LeadConnector forms) is documented as a manual GoHighLevel configuration step in the SUMMARY
    - The project builds successfully
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` succeeds without errors
2. Search entire `src/` directory for `info@10xvelocity.com` -- zero results
3. Search `src/components/landing/Footer.tsx` for `2024` -- zero results
4. Search `src/components/landing/Footer.tsx` for `<Link to="https://` -- zero results
5. `npm run dev` starts, footer shows current year, and external resource links open in new tabs
</verification>

<success_criteria>
- All email addresses across the site read info@10xvelocity.ai
- Footer copyright year is dynamic (currently shows 2026)
- Footer external links use proper `<a>` tags with noopener noreferrer
- No React Router `<Link>` components are used for external URLs
- FIX-02 (LeadConnector forms) is documented as requiring manual GoHighLevel admin action
- The project builds and runs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-crawlability/01-02-SUMMARY.md`
</output>
