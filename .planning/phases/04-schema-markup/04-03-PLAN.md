---
phase: 04-schema-markup
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/pages/case-studies/InnesYoung.tsx
  - src/pages/case-studies/ECatalyst.tsx
  - src/pages/case-studies/HillcrestPartners.tsx
  - src/pages/case-studies/CatalystGroup.tsx
  - src/pages/case-studies/DirectorOfMarketing.tsx
  - src/pages/case-studies/BirchwoodRealEstate.tsx
  - src/pages/case-studies/GovBrokers.tsx
  - src/pages/case-studies/Inspyrd.tsx
  - src/pages/case-studies/TransportationDirector.tsx
  - src/pages/CaseStudies.tsx
  - src/pages/About.tsx
  - src/pages/Contact.tsx
  - src/pages/Blog.tsx
  - src/pages/SavingsCalculator.tsx
  - src/pages/IndustryTools.tsx
  - src/pages/PowerAutomate.tsx
  - src/pages/Prototypes.tsx
  - src/pages/LexiFile.tsx
  - src/pages/LunchAndLearn.tsx
  - src/pages/OnboardingForm.tsx
  - src/pages/AIGuideCertification.tsx
  - src/pages/PrivacyPolicy.tsx
  - src/pages/TermsOfService.tsx
autonomous: true

must_haves:
  truths:
    - "Each of the 9 case study pages contains valid Article JSON-LD with headline, description, author, publisher, and image"
    - "Each of the 9 case study pages contains BreadcrumbList JSON-LD (Home > Case Studies > [Name])"
    - "All remaining interior pages (non-homepage, non-service pages) contain BreadcrumbList JSON-LD"
    - "Article schema headline matches the page's existing Helmet title (minus ' | 10x Velocity' suffix)"
    - "Article schema description matches the page's existing meta description"
  artifacts:
    - path: "src/pages/case-studies/InnesYoung.tsx"
      provides: "Article + BreadcrumbList JSON-LD"
      contains: "helmetJsonLdProp"
    - path: "src/pages/case-studies/TransportationDirector.tsx"
      provides: "Article + BreadcrumbList JSON-LD"
      contains: "helmetJsonLdProp"
    - path: "src/pages/About.tsx"
      provides: "BreadcrumbList JSON-LD"
      contains: "breadcrumbJsonLd"
    - path: "src/pages/Contact.tsx"
      provides: "BreadcrumbList JSON-LD"
      contains: "breadcrumbJsonLd"
  key_links:
    - from: "src/pages/case-studies/InnesYoung.tsx"
      to: "src/schemas/organization.ts"
      via: "import BUSINESS_DATA for publisher"
      pattern: "import.*BUSINESS_DATA"
    - from: "src/pages/case-studies/InnesYoung.tsx"
      to: "src/schemas/breadcrumbs.ts"
      via: "import breadcrumbJsonLd"
      pattern: "import.*breadcrumbJsonLd"
    - from: "src/pages/About.tsx"
      to: "src/schemas/breadcrumbs.ts"
      via: "import breadcrumbJsonLd"
      pattern: "import.*breadcrumbJsonLd"
---

<objective>
Add Article JSON-LD to all 9 case study pages, and BreadcrumbList JSON-LD to all remaining interior pages that do not yet have it (case studies, about, contact, blog, calculator, tools, etc.).

Purpose: Completes SCHM-05 (Article/CaseStudy schema on case studies) and SCHM-03 (BreadcrumbList on ALL interior pages). After this plan, every page on the site has appropriate schema markup.

Output: All non-service interior pages have BreadcrumbList JSON-LD. All case study pages additionally have Article JSON-LD.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-schema-markup/04-RESEARCH.md
@.planning/phases/04-schema-markup/04-01-SUMMARY.md

Key files to read:
@src/schemas/organization.ts (BUSINESS_DATA constant)
@src/schemas/breadcrumbs.ts (breadcrumbJsonLd helper)
@src/pages/case-studies/InnesYoung.tsx (reference case study pattern - has results[], solutions[], Helmet)
@src/App.tsx (complete route list for breadcrumb mapping)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Article + BreadcrumbList JSON-LD on all 9 case study pages</name>
  <files>
    src/pages/case-studies/InnesYoung.tsx
    src/pages/case-studies/ECatalyst.tsx
    src/pages/case-studies/HillcrestPartners.tsx
    src/pages/case-studies/CatalystGroup.tsx
    src/pages/case-studies/DirectorOfMarketing.tsx
    src/pages/case-studies/BirchwoodRealEstate.tsx
    src/pages/case-studies/GovBrokers.tsx
    src/pages/case-studies/Inspyrd.tsx
    src/pages/case-studies/TransportationDirector.tsx
  </files>
  <action>
For each of the 9 case study files, add Article + BreadcrumbList JSON-LD to the existing `<Helmet>` block via the `script` prop.

**Imports to add to each file:**
```ts
import { helmetJsonLdProp } from "react-schemaorg";
import { BUSINESS_DATA } from "@/schemas/organization";
import { breadcrumbJsonLd } from "@/schemas/breadcrumbs";
```

**Script prop pattern for each case study:**
```tsx
<Helmet
  script={[
    helmetJsonLdProp<any>({
      "@context": "https://schema.org",
      "@type": "Article",
      headline: "{Page title WITHOUT ' | 10x Velocity' suffix}",
      description: "{Exact meta description from existing Helmet}",
      author: {
        "@type": "Organization",
        name: BUSINESS_DATA.name,
        url: BUSINESS_DATA.url,
      },
      publisher: {
        "@type": "Organization",
        name: BUSINESS_DATA.name,
        logo: {
          "@type": "ImageObject",
          url: BUSINESS_DATA.logo,
        },
      },
      image: BUSINESS_DATA.image,
      mainEntityOfPage: {
        "@type": "WebPage",
        "@id": "https://10xvelocity.ai/case-studies/{slug}",
      },
    }),
    breadcrumbJsonLd([
      { name: "Home", path: "/" },
      { name: "Case Studies", path: "/case-studies" },
      { name: "{Client/Case Study Name}", path: "/case-studies/{slug}" },
    ]),
  ]}
>
```

**Per-file specifics (READ each file to extract the exact title and description):**

| File | headline (title minus suffix) | Breadcrumb name | Slug |
|------|------|------|------|
| InnesYoung.tsx | "Innes & Young AI PR Case Study" | "Innes & Young" | innes-young |
| ECatalyst.tsx | Read from Helmet title | "eCatalyst" | ecatalyst |
| HillcrestPartners.tsx | Read from Helmet title | "Hillcrest Partners" | hillcrest-partners |
| CatalystGroup.tsx | Read from Helmet title | "Catalyst Group" | catalyst-group |
| DirectorOfMarketing.tsx | Read from Helmet title | "Director of Marketing" | director-of-marketing |
| BirchwoodRealEstate.tsx | Read from Helmet title | "Birchwood Real Estate" | birchwood-real-estate |
| GovBrokers.tsx | Read from Helmet title | "GovBrokers" | govbrokers |
| Inspyrd.tsx | Read from Helmet title | "Inspyrd" | inspyrd |
| TransportationDirector.tsx | Read from Helmet title | "Transportation Director" | transportation-director |

**Critical:** READ each file to get the exact `<title>` and `<meta name="description">` values. The Article headline should be the title WITHOUT the " | 10x Velocity" suffix. The description should be the exact meta description content.

**Do NOT include datePublished or dateModified** -- no publication dates exist in the codebase. Omitting is valid per Google docs.

Do NOT create a second Helmet block. Add `script={[...]}` to the existing `<Helmet>` tag.
  </action>
  <verify>
    - `npm run build` succeeds
    - Spot-check 2-3 case study pages in dev server to confirm `<script type="application/ld+json">` tags appear with Article and BreadcrumbList data
    - Article headline matches the visible page title (not the full Helmet title with suffix)
    - BreadcrumbList has 3 items: Home, Case Studies, [specific case study]
    - Publisher has logo as ImageObject
  </verify>
  <done>All 9 case study pages render Article JSON-LD with headline, description, author, publisher, image, and mainEntityOfPage. All 9 have BreadcrumbList JSON-LD. SCHM-05 fulfilled.</done>
</task>

<task type="auto">
  <name>Task 2: BreadcrumbList JSON-LD on all remaining interior pages</name>
  <files>
    src/pages/CaseStudies.tsx
    src/pages/About.tsx
    src/pages/Contact.tsx
    src/pages/Blog.tsx
    src/pages/SavingsCalculator.tsx
    src/pages/IndustryTools.tsx
    src/pages/PowerAutomate.tsx
    src/pages/Prototypes.tsx
    src/pages/LexiFile.tsx
    src/pages/LunchAndLearn.tsx
    src/pages/OnboardingForm.tsx
    src/pages/AIGuideCertification.tsx
    src/pages/PrivacyPolicy.tsx
    src/pages/TermsOfService.tsx
  </files>
  <action>
Add BreadcrumbList JSON-LD to every interior page that does not already have it. The homepage (Index.tsx) does NOT get breadcrumbs (it IS the root). Service pages were handled in Plan 02.

**Import to add to each file:**
```ts
import { breadcrumbJsonLd } from "@/schemas/breadcrumbs";
```

**For each file, add `script` prop to the existing `<Helmet>` block:**

| File | Route | Breadcrumb Trail |
|------|-------|-----------------|
| CaseStudies.tsx | /case-studies | Home > Case Studies |
| About.tsx | /about | Home > About |
| Contact.tsx | /contact | Home > Contact |
| Blog.tsx | /blog | Home > Blog |
| SavingsCalculator.tsx | /savings-calculator | Home > Savings Calculator |
| IndustryTools.tsx | /industry-tools | Home > Industry Tools |
| PowerAutomate.tsx | /power-automate | Home > Power Automate |
| Prototypes.tsx | /prototypes | Home > Prototypes |
| LexiFile.tsx | /lexi-file | Home > Lexi File |
| LunchAndLearn.tsx | /events/lunch-and-learn | Home > Events > Lunch & Learn |
| OnboardingForm.tsx | /demo | Home > Demo |
| AIGuideCertification.tsx | /programs/ai-guide-certification | Home > Programs > AI Guide Certification |
| PrivacyPolicy.tsx | /privacy-policy | Home > Privacy Policy |
| TermsOfService.tsx | /terms-of-service | Home > Terms of Service |

**Example for About.tsx:**
```tsx
<Helmet
  script={[
    breadcrumbJsonLd([
      { name: "Home", path: "/" },
      { name: "About", path: "/about" },
    ]),
  ]}
>
  {/* ...existing title, meta, OG tags... */}
</Helmet>
```

**For LunchAndLearn.tsx** (nested under /events/):
```tsx
breadcrumbJsonLd([
  { name: "Home", path: "/" },
  { name: "Events", path: "/events/lunch-and-learn" },
  { name: "Lunch & Learn", path: "/events/lunch-and-learn" },
])
```
Note: There is no /events index page, so Events in the breadcrumb points to the same URL. Alternatively, use a 2-item breadcrumb: Home > Lunch & Learn. Use the 2-item version since /events is not a real route.

**Corrected LunchAndLearn.tsx:**
```tsx
breadcrumbJsonLd([
  { name: "Home", path: "/" },
  { name: "Lunch & Learn", path: "/events/lunch-and-learn" },
])
```

**For AIGuideCertification.tsx** (nested under /programs/):
Same situation -- /programs is not a real route. Use 2-item breadcrumb:
```tsx
breadcrumbJsonLd([
  { name: "Home", path: "/" },
  { name: "AI Guide Certification", path: "/programs/ai-guide-certification" },
])
```

**Note about BlogPost component:** The dynamic `/blog/:id` route uses `src/components/blog/BlogPost.tsx` (a component, not a page). It already has a Helmet block per blog post. Add breadcrumb there too:
```tsx
breadcrumbJsonLd([
  { name: "Home", path: "/" },
  { name: "Blog", path: "/blog" },
  { name: "{post.title}", path: `/blog/${id}` },
])
```
Read the BlogPost component to see how `id` and `post` data are available, then add the breadcrumb import and script prop accordingly.

Do NOT create second Helmet blocks. Always add `script={[...]}` to existing `<Helmet>` tags.
  </action>
  <verify>
    - `npm run build` succeeds
    - Spot-check 3-4 pages in dev server (About, Contact, CaseStudies index, a blog post) to confirm BreadcrumbList JSON-LD appears in page source
    - Each BreadcrumbList starts with Home (position 1) and ends with the current page
    - Last ListItem in each breadcrumb omits the `item` property (per Google spec)
    - No page other than the homepage lacks BreadcrumbList JSON-LD
  </verify>
  <done>All 14 remaining interior pages plus the dynamic BlogPost component have BreadcrumbList JSON-LD. Combined with Plans 01 and 02, every page on the site has appropriate schema markup. SCHM-03 fully fulfilled.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. All 9 case study pages have Article JSON-LD (headline, description, author, publisher, image)
3. All 9 case study pages have BreadcrumbList JSON-LD (Home > Case Studies > [Name])
4. All 14 other interior pages have BreadcrumbList JSON-LD
5. BlogPost dynamic component has BreadcrumbList JSON-LD (Home > Blog > [Post Title])
6. No BreadcrumbList has fewer than 2 items
7. Last breadcrumb item omits `item` property in every BreadcrumbList
</verification>

<success_criteria>
- Every case study page renders Article + BreadcrumbList JSON-LD
- Every interior page (non-homepage) renders BreadcrumbList JSON-LD
- npm run build succeeds with zero errors
- SCHM-03 and SCHM-05 fully complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-schema-markup/04-03-SUMMARY.md`
</output>
